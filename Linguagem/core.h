#include <stdlib.h>
#include <stdio.h>
struct lexica {
	
const char *comando[2] = {"imprimir","entrada"};
};
typedef struct lexica Lexica;


int parse(FILE *arquivo){
	int = 0;
	for(i=0; i=EOF;i++){
		line = fgetc(arquivo);
	}
}
